# workers/document

Target home for the Python document adapter placeholder created by `A007`.

Expected future entry:

```powershell
python workers/document/worker.py --help
```

This directory is intentionally a placeholder until A007 creates the worker contract.
