# Apps

Runtime applications live here after P0 scaffold tasks:

- `api`: ASP.NET Core modular monolith API, created in A002.
- `web`: React + TypeScript + Vite + Ant Design UI, created in A003.

Do not add business implementation before A002/A003 create the runnable project entries.
