name: repo-preflight

permissions:
  contents: read

on:
  push:
    paths:
      - 'README.md'
      - 'apps/**'
      - 'configs/**'
      - 'docs/**'
      - 'global.json'
      - 'schemas/**'
      - 'sources/**'
      - 'tasks/**'
      - 'tools/**'
      - 'workers/**'
      - '.github/workflows/repo-preflight.yml'
  pull_request:
    paths:
      - 'README.md'
      - 'apps/**'
      - 'configs/**'
      - 'docs/**'
      - 'global.json'
      - 'schemas/**'
      - 'sources/**'
      - 'tasks/**'
      - 'tools/**'
      - 'workers/**'
      - '.github/workflows/repo-preflight.yml'
  workflow_dispatch:

jobs:
  preflight:
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: apps/web/package-lock.json

      - name: Restore dependencies
        shell: pwsh
        run: |
          dotnet restore tests/api/K12QuestionGraph.Api.Tests/K12QuestionGraph.Api.Tests.csproj
          npm --prefix apps/web ci

      - name: Run stateless verification
        shell: pwsh
        run: >
          .\tools\run-verification.ps1
          -Profile Quick
          -ReportRoot tmp\ci\repo-preflight

      - name: Upload preflight report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: repo-preflight-report
          path: |
            tmp/ci/repo-preflight/**
          if-no-files-found: error
